--Creating the relevant table in pgAdmin with the corresponding column names

CREATE TABLE insurance (
    age INTEGER,
    sex TEXT,
    bmi NUMERIC,
    children INTEGER,
    smoker TEXT,
    region TEXT,
    charges NUMERIC
);
